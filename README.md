Local setting:
- make sure docker is running
- `npm run docker-run`
- default port is 8000 (`http://localhost:8000`)

4 endpoints:
- GET `/inventories`
- POST `/inventory`
- PATCH `/inventory`
- DELETE `/inventory`
